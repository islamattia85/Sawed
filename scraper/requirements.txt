requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2
pdfplumber==0.11.0
# certifi ships with requests and cryptography with pdfplumber, but the TLS
# chain-completion in sources.py depends on both directly, so pin them.
certifi==2024.7.4
cryptography==43.0.1
# Flogas renders its rate tables in the browser; only a headless page can read
# them. Lazy-imported in render.py, so the browser is needed only where a step
# runs `playwright install chromium` (the scrape and diagnose jobs), not for the
# unit tests.
playwright==1.47.0
